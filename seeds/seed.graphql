mutation {
  heng: createUserProfile(data: {
    name:"Heng"
  }) {
    id
    name
  }

  paine: createUserProfile(data: {
    name:"Paine"
  }) {
    id
    name
  }

  ashley: createUserProfile(data: {
    name:"Ashley"
  }) {
    id
    name
  }
}